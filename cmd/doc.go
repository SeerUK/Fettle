// Package cmd is the home for all binaries to be built. Each folder will have a main.go file in.
// To build these, run `$ go build` in the folder to produce a binary with the same name as the
// folder.
package cmd
